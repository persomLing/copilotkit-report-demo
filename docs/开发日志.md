# CopilotKit 研报工作台 Demo 开发日志

## 2026-07-01

### 1. 项目分析

- 分析项目的方向：用 React 页面状态和动作注册，让 AI 控制已有业务 UI。
- 判断 Demo 本质是“状态派”：AI 不直接点 DOM，而是通过状态、工具和业务 action 改变页面。
- 明确 CopilotKit 更适合旧 React 项目渐进改造。

### 2. CopilotKit Demo 初始化

- 创建 `copilotkit-report-demo`。
- 使用 Vite + React。
- 引入：
  - `@copilotkit/react-core`
  - `@copilotkit/runtime`
  - `@ai-sdk/openai`
  - `express`
  - `zod`
- 建立前端工作台页面和后端 Runtime。

### 3. 模型接入

- 接入 DeepSeek OpenAI-compatible endpoint。
- 使用 `.env` 存储后端密钥。
- 前端只访问 `/api/copilotkit`，不暴露模型密钥。

### 4. 研报工作台基础能力

- 构造模拟研报数据。
- 支持筛选：
  - 关键词
  - 券商
  - 评级
  - 一级分类
  - 二级分类
  - 日期区间
- 支持排序：
  - 日期
  - 评级
  - 分数
  - 热度
- 支持分页、详情、选择、导出。

### 5. Copilot 前端工具注册

注册工具：

- `resolveSecondaryFilterIntent`
- `loadSecondaryFilterOptions`
- `setReportFilter`
- `loadReports`
- `clearReportFilter`
- `openReportDetail`
- `selectReports`
- `exportSelectedReports`
- `exportVisibleReports`

### 6. UI 改造

- 将默认 CopilotKit 大面板改成悬浮按钮 + 弹窗。
- 优化筛选栏。
- 二级筛选只在选择具体一级分类后出现。
- 排序移动到表头。
- 增加清空条件按钮。
- 增加导出按钮。
- 优化整体视觉和交互。

### 7. 数据量和复杂场景增强

- 扩充模拟研报数据。
- 增加多种金融一级和二级分类。
- 增加二级联动流程。
- 增加手动操作和 AI 操作混用场景。

### 8. 导出状态问题修复

问题：

- AI 选中当前页后立即导出，导出工具读到旧的 `selectedIds`。

修复：

- 增加 `reportsRef`。
- 增加 `selectedIdsRef`。
- 让工具 handler 读取 ref。
- 手动勾选、AI 选中、查询刷新都同步 ref。

### 9. 当前页导出语义优化

问题：

- 用户说“导出当前页面数据”，AI 可能先调用选择，再导出选中项。

修复：

- 新增 `exportVisibleReports`。
- 当前页导出不需要先选中。
- 选中项导出和当前页导出拆成两个工具。

### 10. 二级筛选语义路由

需求：

- 用户直接说“可转债”等二级词时，AI 不能直接设置二级筛选。
- AI 需要有金融背景，但真实二级数据要通过接口获取。

实现：

- 增加 `SECONDARY_TERM_HINTS` 金融相似词典。
- 增加 `resolveSecondaryTermHint`。
- 增加 `resolveSecondaryFilterIntent` 工具。
- 页面上下文不再直接暴露 `secondaryOptions` 数组。
- `setReportFilter` 增加 `secondaryConfirmedByUser` 硬校验。

### 11. 接口等待和重试

需求：

- 接口请求有耗时。
- AI 应等待接口结果。
- 接口失败后自动重试一次。
- 重试仍失败时告知用户。

实现：

- 增加 `requestSecondaryOptions`。
- `fetchSecondaryOptions` 统一处理等待、重试、失败。
- `loadSecondaryFilterOptions` 返回结构化结果：
  - `ok`
  - `attempts`
  - `error`
  - `nextStep`

### 12. 注释补充

- 给核心代码增加注释：
  - provider 初始化
  - Runtime 模型接入
  - 状态 ref 设计
  - 查询结果同步
  - 工具注册
  - 导出逻辑
  - 二级筛选依赖关系

### 13. Docker 化

- 增加 `Dockerfile`。
- 增加 `docker-compose.yml`。
- 增加 `.dockerignore`。
- 增加 `.gitignore`。
- 后端 Express 增加静态托管 `dist`。
- Docker 版只暴露 `4000`，同时提供前端和 `/api/copilotkit`。

### 14. 验证记录

执行过的验证：

```bash
npm run build
```

结果：

- Vite 构建成功。
- 存在 chunk size warning，这是 CopilotKit/mermaid 等依赖体积导致的构建警告，不影响运行。

后端健康检查：

```bash
Invoke-RestMethod http://localhost:4000/health
```

结果：

```json
{ "ok": true }
```

## 待后续增强

- 将 action 审计日志持久化到后端。
- 给每次工具调用增加 requestId。
- 对接口失败重试增加退避策略。
- 将金融词典迁移到真实接口或向量召回。
- 增加 Playwright 自动化回归。
- 对导出等敏感动作改成更正式的审批弹窗。

### 15. AG-UI 软打断与共享操作日志

本次根据业务语义重新定义“打断”：

- 用户打断只停止 AI 当前 run 的继续输出和后续工具规划。
- 已经发出的前端业务请求不再取消。
- 二级筛选接口、研报加载等请求完成后仍然写入共享页面状态。
- 这样更接近真实 B 端系统：进入后端/接口执行阶段的动作默认继续完成，避免半路取消导致业务状态不确定。

实现调整：

- 保留 `agent.abortRun()`。
- 移除前端业务请求对 `AbortController` 的依赖。
- 将运行中提示从“请求可取消”调整为“AI 后续操作可打断，已发请求继续完成”。
- 后端 agent prompt 同步更新，避免 AI 误以为业务请求被取消。

### 16. 结构化 operationLog

新增结构化操作日志，用于支撑用户追问：

- “我刚才做了什么？”
- “AI 刚才做了什么？”
- “现在页面是怎么变成这样的？”

日志字段：

- `actor`: `user` / `ai` / `system`
- `type`: 动作类型，例如 `setFilterField`、`loadReports`、`openReportDetail`
- `status`: `running` / `completed` / `failed` / `interrupted`
- `summary`: 给人看的摘要
- `payload`: 结构化参数和结果
- `createdAt`: 时间

接入范围：

- 用户手动筛选、加载、清空、导出、翻页、排序、勾选、打开详情。
- AI 设置筛选、加载列表、打开详情、选择研报、导出。
- 系统接口等待、成功、失败、重试。

Copilot 上下文新增 `recentOperations`，AI 回答历史操作问题时必须优先基于该字段，而不是依赖对话记忆猜测。

### 17. 筛选维度增强

新增筛选条件：

- 风险等级：全部 / 低 / 中 / 高
- 分析师
- 最低评分
- 最低热度

涉及改动：

- `reportStore.js`
  - 新增 `RISKS`、`ANALYSTS`
  - `normalizeFilter` 支持 `risk`、`analyst`、`scoreMin`、`readCountMin`
  - `queryReports` 支持新增条件过滤
- `App.jsx`
  - 默认筛选条件补齐新增字段
  - `setReportFilter` 工具 schema 支持新增字段
  - 筛选栏增加对应控件
  - `visibleReports` 上下文增加 `risk`
- `styles.css`
  - 筛选栏调整为更弹性的六列网格，适配更多筛选项

### 18. 技术方案与旧项目审计 Skill 更新

本次根据 Demo 开发过程和前面的技术讨论，补齐了可复用的方法论文档：

- 新增 `docs/React项目AI化技术方案与旧项目审计模板.md`
  - 总结状态派 AI 改造架构。
  - 明确 CopilotKit/AG-UI、页面适配层、业务 service、operationLog 的职责边界。
  - 固化“软打断”语义：打断 AI 当前 run 和后续工具规划，不取消已经发出的业务请求。
  - 提供旧 React 项目 AI 化审计 Prompt，可直接喂给 AI 分析项目是否适合改造。

同步更新本机 Codex skill：

- `C:\Users\Administrator\.codex\skills\react-ai-readiness-audit\SKILL.md`
  - 增加 CopilotKit/AG-UI、旧项目状态整洁度、页面适配层、打断语义、操作日志等审计要求。
  - 强化输出模板，要求给出路线选择、风险、阶段计划和待确认问题。
- `C:\Users\Administrator\.codex\skills\react-ai-readiness-audit\agents\openai.yaml`
  - 更新默认 prompt，让后续调用该 skill 时默认覆盖状态/API、打断、日志和 CopilotKit 适配判断。

校验：

```bash
python -X utf8 C:\Users\Administrator\.codex\skills\.system\skill-creator\scripts\quick_validate.py C:\Users\Administrator\.codex\skills\react-ai-readiness-audit
```

结果：

```text
Skill is valid!
```

### 19. 二级筛选确认规则优化

问题：

- 用户输入“先加载股票研究的二级筛选，再只看电力设备里华泰证券的买入报告，按热度降序”时，AI 仍然追问“电力设备是二级筛选还是关键词”。
- 这不是 CopilotKit 无法区分，而是 Demo 规则过于保守：只要出现疑似二级词，就强制追问。

修复：

- 后端 `server/index.js`
  - 增加规则：如果用户同一句话已经明确一级分类，并要求先加载该一级的二级筛选，再指定二级值，视为用户已经确认二级筛选意图，不需要再次追问。
  - 正确流程变为：`loadSecondaryFilterOptions` -> 校验候选项 -> `setReportFilter(secondaryConfirmedByUser=true)` -> `loadReports`。
- 前端 `src/App.jsx`
  - 更新 `requiredWorkflowForSecondaryFilter` 和 `useAgentContext` 描述。
  - `resolveSecondaryFilterIntent` 增加 `explicitPrimaryCategory` 参数。
  - `loadSecondaryFilterOptions` 描述允许“已明确一级分类”的场景直接请求候选项。
  - 修复 `setReportFilter` 在一级和二级同次设置时误把二级重置为“全部”的问题。
- 数据词典 `src/reportStore.js`
  - 补充 `电力设备`、`机器人`、`消费电子`、`医药生物`、`银行`等股票研究方向。
  - `resolveSecondaryTermHint(term, explicitPrimaryCategory)` 在用户已明确一级分类时返回 `needsClarification=false`。

验证：

```bash
npm run build
```

结果：

```text
✓ built
```

词典验证：

```js
resolveSecondaryTermHint("电力设备", "股票研究")
```

结果：

```json
{
  "matched": true,
  "explicitPrimaryCategory": "股票研究",
  "primaryCandidates": ["股票研究"],
  "needsClarification": false
}
```

### 20. 基金主题词与行业二级词歧义优化

问题：

- 用户输入“评分不低于80 半导体相关的基金”时，AI 追问“股票研究下的半导体方向，还是基金研究”。
- 对业务用户来说，“基金”已经明确表达对象是基金研究；“半导体”在这里更像主题关键词，不应该被强行当成股票研究二级筛选。

修复：

- 后端 `server/index.js`
  - 增加语义优先级：当用户说“半导体相关的基金”“新能源相关基金”“AI 主题基金”时，`基金` 优先表示 `primaryCategory=基金研究`，行业/主题词作为 `keyword`。
  - 增加规则：评分不低于、评分至少、80 分以上映射到 `scoreMin`，不要误用评级 `rating`。
- 前端 `src/App.jsx`
  - 在 `useAgentContext` 和二级筛选 workflow 中同步上述规则，避免模型被“半导体是股票研究二级分类”的旧规则带偏。
- 数据 `src/reportStore.js`
  - 给基金研究模拟数据增加主题覆盖词，例如半导体、新能源、人工智能、医药生物等。
  - 这样 `primaryCategory=基金研究 + keyword=半导体 + scoreMin=80` 能查到模拟结果。

验证：

```js
queryReports({
  primaryCategory: "基金研究",
  keyword: "半导体",
  scoreMin: 80,
  orderBy: "score",
  direction: "desc",
  pageSize: 10
})
```

结果：

```text
total: 17
```

### 21. 追问策略改为置信度决策

问题：

- 仅靠“疑似二级词默认追问”会让 AI 像表单校验器，用户容易觉得不如手动筛选。
- 对筛选、排序、关键词检索这类可撤销动作，应该尽量先做，再允许用户纠正。

策略调整：

- 高置信度：直接执行。
- 中置信度：先执行，并说明当前假设。
- 低置信度：才追问。
- 敏感动作，例如导出、删除、提交，仍然必须确认。

实现：

- `src/reportStore.js`
  - 新增 `resolveReportFilterIntent(userText)`。
  - 返回 `confidence`、`action`、`filterPatch`、`notes`、`nextSteps`。
  - 修复中文子串误判：`评分不低于80` 不再把“低”识别为风险等级，只有“低风险/风险低”才映射 `risk=低`。
  - `评分不低于80` 只映射 `scoreMin=80`，不自动触发按评分排序。
- `src/App.jsx`
  - 新增 CopilotKit 前端工具 `resolveReportFilterIntent`。
  - 在 `useAgentContext` 中要求筛选类自然语言优先调用该工具，再根据 `action` 决定执行或追问。
- `server/index.js`
  - 后端 Agent prompt 改为优先使用整体意图解析工具。
  - 明确不要因为出现疑似二级词就默认追问。

典型结果：

```js
resolveReportFilterIntent("评分不低于80 半导体相关的基金")
```

返回：

```json
{
  "confidence": "high",
  "action": "execute",
  "filterPatch": {
    "scoreMin": 80,
    "primaryCategory": "基金研究",
    "keyword": "半导体"
  }
}
```

```js
resolveReportFilterIntent("看半导体")
```

返回：

```json
{
  "confidence": "medium",
  "action": "execute_with_note",
  "filterPatch": {
    "primaryCategory": "股票研究",
    "secondaryCategory": "半导体",
    "secondaryConfirmedByUser": true
  }
}
```

```js
resolveReportFilterIntent("筛一下")
```

返回：

```json
{
  "confidence": "low",
  "action": "clarify",
  "question": "请补充想筛选的对象，例如基金、股票研究、债券研究、券商、评分或关键词。"
}
```

### 22. 新增用户记忆与系统规则沉淀

背景：

- 只靠固定 prompt 无法持续吸收开发过程中的经验。
- 用户反馈里有两类信息需要分开处理：
  - 当前用户自己的偏好，例如“别一直追问，能执行就先执行”。
  - 可复用的系统规则，例如“半导体相关基金应走基金研究 + keyword，不应走股票二级行业”。
- 如果把所有反馈都直接写成全局规则，会污染系统；如果完全不沉淀，AI 会反复犯同类错误。

设计：

- 新增两层知识体系：
  - `userMemory`：当前用户偏好、习惯、纠错。
  - `systemRules`：包含 `approved` 已生效规则、`candidates` 待审核规则、`caseLog` 学习案例。
- 规则优先级：
  1. 前端工具返回值和业务接口结果。
  2. 已批准系统规则。
  3. 当前用户记忆。
  4. 当前对话上下文。
  5. 模型常识。
- 待审核规则只能展示和等待人工批准，不能直接当事实执行。

实现：

- `src/memoryStore.js`
  - 新增 localStorage 持久化。
  - 新增默认规则：评分阈值、主题基金、明确一级二级时不追问。
  - 新增 `extractRuleCandidateFromFeedback`，把一次纠错启发式提炼成候选规则。
- `src/App.jsx`
  - 新增 `knowledgeBase` 状态。
  - 通过 `useAgentContext` 暴露 `learningMemory`。
  - 新增 CopilotKit 工具：
    - `recordUserMemory`
    - `recordLearningCase`
    - `proposeSystemRule`
    - `approveSystemRule`
    - `dismissSystemRule`
    - `forgetUserMemory`
  - 新增“知识沉淀”面板，展示用户记忆、已生效规则、待审核规则。
- `server/index.js`
  - Agent prompt 增加学习体系说明。
  - 明确 `pendingRuleCandidates` 不能直接执行。
- `README.md`
  - 补充学习体系说明和技术设计入口。
- `docs/技术设计.md`
  - 新增整体架构、工具链路、打断策略、两层学习体系说明。

注意：

- 当前 demo 使用 localStorage，生产环境应迁移到后端存储。
- 批准系统规则应接权限、审计和灰度发布。
- 规则抽取不应完全交给模型自动生效，至少要经过人工审核或离线评估。

### 23. 补充多模块多筛选风格设计

背景：

- 当前 demo 只有研报筛选一个模块。
- 真实系统中可能同时存在研报、基金、基金经理、组合、产品等多个模块。
- 每个模块的筛选字段、业务语义、接口链路都不同，不能让大模型靠常识自由猜。

补充设计：

- 在 `docs/技术设计.md` 新增“多模块多筛选风格扩展”。
- 明确模型区分模块依赖四类信息：
  - 当前页面状态。
  - 模块语义描述。
  - 模块筛选 schema。
  - 模块独立工具。
- 推荐 `AI Shell + Module Adapter` 架构：
  - `ReportModule`
  - `FundModule`
  - `ManagerModule`
  - `PortfolioModule`
- 推荐两段式上下文：
  1. 先暴露模块目录。
  2. 通过 `resolveModuleIntent(userText)` 选择模块。
  3. 再暴露选中模块的详细 schema、状态和工具。
- 多模块规则优先级：

```text
工具/接口事实 > 全局规则 > 当前模块规则 > 用户记忆 > 对话上下文 > 模型常识
```

结论：

- 不要做一个超大的 `setAnyFilter`。
- 模块越多，越要把 state、schema、tools、workflowRules 分模块注册。
- 多模块场景下，AI 的关键能力不是“猜得更准”，而是被清晰的模块边界和工具约束住。
